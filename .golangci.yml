run:
  go: '1.24'

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - gofmt
    - goimports
    - misspell

linters-settings:
  goimports:
    local-prefixes: github.com/edgeai-platform/ai-edge

issues:
  exclude-dirs:
    - api/gen
